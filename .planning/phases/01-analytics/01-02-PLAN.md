---
phase: 01-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - public/index.html
  - public/leistungen.html
  - public/leistungen-kontaktlinsen.html
  - public/leistungen-nachtlinsen.html
  - public/leistungen-beratung-kurzsichtigkeit.html
  - public/leistungen-visuelle-reha.html
  - public/team.html
  - public/kontakt.html
  - public/datenschutz.html
  - public/impressum.html
autonomous: true
requirements:
  - ANAL-02
  - ANAL-03

must_haves:
  truths:
    - "All 10 HTML files contain the Umami tracking script with the correct data-website-id UUID"
    - "No HTML file contains the dead Google Analytics comment block"
    - "A test page visit on bessersehen.la appears in the Umami realtime dashboard within 60 seconds"
    - "No cookie banner is present or required on the website"
  artifacts:
    - path: "public/index.html"
      provides: "Tracking script before </head>"
      contains: "analytics.bessersehen.la/script.js"
    - path: "public/datenschutz.html"
      provides: "Tracking script (legal pages also tracked)"
      contains: "analytics.bessersehen.la/script.js"
    - path: "public/impressum.html"
      provides: "Tracking script (legal pages also tracked)"
      contains: "analytics.bessersehen.la/script.js"
  key_links:
    - from: "All 10 HTML files (script tag)"
      to: "https://analytics.bessersehen.la/script.js"
      via: "src attribute in defer script tag"
      pattern: "src=\"https://analytics.bessersehen.la/script.js\""
    - from: "Script tag data-domains attribute"
      to: "bessersehen.la production domain"
      via: "data-domains restriction"
      pattern: "data-domains=\"bessersehen.la\""
---

<objective>
Insert the Umami tracking script into all 10 HTML files, remove the dead Google Analytics comment block from each file, and verify that real page visits appear in the Umami dashboard.

Purpose: ANAL-02 requires tracking on all HTML pages. ANAL-03 requires verified data collection. This plan closes both requirements after Plan 01-01 has made Umami operational and provided the website UUID.

Output: All 10 HTML files updated with Umami script, dead GA code removed, deployment pushed to server, and a live tracking test confirmed.
</objective>

<execution_context>
@/Users/claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-analytics/01-01-SUMMARY.md
@.planning/phases/01-analytics/01-RESEARCH.md

<interfaces>
<!-- Key information from Plan 01-01 that this plan depends on -->

From Plan 01-01-SUMMARY.md:
- Umami website UUID for bessersehen.la (a UUID string like: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx)
  READ THIS FROM THE SUMMARY FILE — it was recorded there after the human completed Task 3

Tracking script pattern (from RESEARCH.md):
```html
<!-- Umami Analytics — cookiefrei, keine personenbezogenen Daten, DSGVO-konform -->
<script
  defer
  src="https://analytics.bessersehen.la/script.js"
  data-website-id="UUID-FROM-SUMMARY"
  data-domains="bessersehen.la"
></script>
```

Dead GA comment to remove (present in all 10 HTML files — may appear as empty comment or include gtag code):
```html
<!-- Global site tag (gtag.js) - Google Analytics -->
```
This comment and any associated gtag script block should be deleted entirely.

Placement: Just before `</head>` closing tag. The script must come after any existing `<script src="vendor/...">` tags.

HTML files to update (10 total, not 8 — datenschutz.html and impressum.html are also in scope):
1. public/index.html
2. public/leistungen.html
3. public/leistungen-kontaktlinsen.html
4. public/leistungen-nachtlinsen.html
5. public/leistungen-beratung-kurzsichtigkeit.html
6. public/leistungen-visuelle-reha.html
7. public/team.html
8. public/kontakt.html
9. public/datenschutz.html
10. public/impressum.html
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Umami Script and Remove Dead GA Code in All 10 HTML Files</name>
  <files>public/index.html, public/leistungen.html, public/leistungen-kontaktlinsen.html, public/leistungen-nachtlinsen.html, public/leistungen-beratung-kurzsichtigkeit.html, public/leistungen-visuelle-reha.html, public/team.html, public/kontakt.html, public/datenschutz.html, public/impressum.html</files>
  <action>
Read the Umami website UUID from `.planning/phases/01-analytics/01-01-SUMMARY.md` before starting.

For each of the 10 HTML files, make two changes:

**Change 1 — Remove the dead GA comment block:**
Find and delete the following comment (and any associated empty lines around it):
```
<!-- Global site tag (gtag.js) - Google Analytics -->
```
If the file also contains a `<script>` block referencing `googletagmanager.com` or `UA-185735048-1`, remove that entire script block too.

**Change 2 — Insert the Umami tracking script:**
Insert the following block just before the `</head>` closing tag (replacing the space where the GA comment was, or at the end of `<head>` if GA comment was the last item):
```html
		<!-- Umami Analytics — cookiefrei, keine personenbezogenen Daten, DSGVO-konform -->
		<script
		  defer
		  src="https://analytics.bessersehen.la/script.js"
		  data-website-id="UUID-HERE"
		  data-domains="bessersehen.la"
		></script>
```
Replace `UUID-HERE` with the actual UUID from the SUMMARY.md (same UUID in all 10 files).

Use consistent indentation matching each file's existing `<head>` section style (tabs in most files).

After editing all 10 files, verify the changes are consistent:
```bash
grep -c "analytics.bessersehen.la" /Users/claude/Projekte/BesserSehenLandshut/public/*.html
# Expected: each file shows count of 1

grep -l "google-analytics\|UA-185735048\|gtag.js" /Users/claude/Projekte/BesserSehenLandshut/public/*.html
# Expected: no output (no files should match)
```
  </action>
  <verify>
    <automated>
# All 10 files have the tracking script
grep -c "analytics.bessersehen.la/script.js" /Users/claude/Projekte/BesserSehenLandshut/public/*.html | grep -v ":1" | wc -l
# Expected: 0 (all files have exactly 1 match)

# No files still have dead GA code
grep -l "google-analytics\|UA-185735048\|gtag\.js" /Users/claude/Projekte/BesserSehenLandshut/public/*.html | wc -l
# Expected: 0

# All files have data-domains set correctly
grep -c "data-domains=\"bessersehen.la\"" /Users/claude/Projekte/BesserSehenLandshut/public/*.html | grep -v ":1" | wc -l
# Expected: 0 (all files have exactly 1 match)
    </automated>
  </verify>
  <done>All 10 HTML files contain the Umami tracking script with the correct UUID and data-domains="bessersehen.la". No file contains the dead Google Analytics comment or any googletagmanager/UA references.</done>
</task>

<task type="auto">
  <name>Task 2: Commit and Deploy HTML Changes, Then Verify Live Tracking</name>
  <files>public/*.html (all 10 files)</files>
  <action>
1. Stage and commit the HTML changes:
```bash
cd /Users/claude/Projekte/BesserSehenLandshut
git add public/index.html public/leistungen.html public/leistungen-kontaktlinsen.html public/leistungen-nachtlinsen.html public/leistungen-beratung-kurzsichtigkeit.html public/leistungen-visuelle-reha.html public/team.html public/kontakt.html public/datenschutz.html public/impressum.html
git commit -m "feat(analytics): Umami-Tracking auf allen 10 Seiten einbinden, toten GA-Code entfernen"
```

2. Push to origin (triggers GitHub Actions deployment to Pollux if configured, or manual rsync):
```bash
git push origin main
```

3. Wait ~60 seconds for deployment to complete, then verify the script is live:
```bash
curl -sf https://bessersehen.la/ | grep "analytics.bessersehen.la"
# Expected: the script tag should appear in the response
```

4. Verify live tracking works by opening https://bessersehen.la in a browser, then checking the Umami Realtime dashboard at https://analytics.bessersehen.la:
- Navigate to the bessersehen.la website in your browser (use a different browser profile or incognito to avoid the `umami.disabled` localStorage flag set during setup)
- Open https://analytics.bessersehen.la and check the Realtime view
- The visit should appear within 30-60 seconds

5. If GitHub Actions deployment is not configured (manual rsync):
```bash
rsync -avz --delete /Users/claude/Projekte/BesserSehenLandshut/public/ pollux:/home/users/bessersehen/www/
```
Then re-verify step 3.

Note: The `data-domains="bessersehen.la"` attribute means the script silently does nothing when loaded from any domain other than bessersehen.la (local dev, staging). Only production visits are tracked.
  </action>
  <verify>
    <automated>
# Script tag present in live production HTML
curl -sf https://bessersehen.la/ | grep -c "analytics.bessersehen.la/script.js"
# Expected: 1

# Tracker script itself is accessible from production domain
curl -sI https://analytics.bessersehen.la/script.js | grep "HTTP"
# Expected: HTTP/2 200

# Git log shows the commit
git -C /Users/claude/Projekte/BesserSehenLandshut log --oneline -1
# Expected: commit starting with "feat(analytics):"
    </automated>
  </verify>
  <done>HTML changes are committed and deployed. The Umami script tag is visible in the live HTML at https://bessersehen.la. A test page visit appears in the Umami Realtime dashboard at https://analytics.bessersehen.la within 60 seconds of the visit. No cookie banner is shown or required.</done>
</task>

</tasks>

<verification>
Full phase verification after both tasks complete:

```bash
# 1. Umami still healthy
curl -sf https://analytics.bessersehen.la/api/heartbeat
# Must return: {"ok":true}

# 2. All 10 local HTML files have tracking script
grep -l "analytics.bessersehen.la/script.js" /Users/claude/Projekte/BesserSehenLandshut/public/*.html | wc -l
# Must return: 10

# 3. No dead GA code remains
grep -rl "google-analytics\|UA-185735048\|gtag\.js" /Users/claude/Projekte/BesserSehenLandshut/public/*.html | wc -l
# Must return: 0

# 4. Script live in production
curl -sf https://bessersehen.la/ | grep -q "analytics.bessersehen.la" && echo "PASS" || echo "FAIL"
# Must return: PASS

# 5. Tracker script accessible
curl -sf https://analytics.bessersehen.la/script.js | head -c 100 | grep -q "function" && echo "PASS" || echo "FAIL"
# Must return: PASS (Umami's compiled JS starts with function/class definitions)
```
</verification>

<success_criteria>
- All 10 HTML files (including datenschutz.html and impressum.html) contain the Umami tracking script with the correct UUID
- The dead Google Analytics comment block is removed from all 10 files
- The changes are committed and pushed to the main branch
- The live site at https://bessersehen.la serves the Umami script tag
- A real page visit on https://bessersehen.la is visible in the Umami dashboard within 60 seconds
- No cookie banner is present on the website
</success_criteria>

<output>
After completion, create `.planning/phases/01-analytics/01-02-SUMMARY.md` documenting:
- Confirmation that all 10 HTML files were updated
- Git commit hash for the tracking script commit
- Confirmation that live tracking was verified (screenshot description or date/time of confirmed visit in Umami)
- Any issues encountered during deployment
</output>
