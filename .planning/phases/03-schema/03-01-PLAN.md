---
phase: 03-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-05

must_haves:
  truths:
    - "Das Optician-Schema auf index.html enthaelt openingHoursSpecification mit closes='16:00' (nicht 18:00)"
    - "Das Optician-Schema enthaelt sameAs mit Facebook-URL UND Google Maps CID-URL"
    - "Das Optician-Schema enthaelt areaServed als City-Array mit Landshut, Ergolding, Rottenburg an der Laaber"
    - "Ein separater WebSite-Schema-Block existiert in index.html mit @type WebSite, name, url, inLanguage de-DE"
    - "Alle bestehenden Felder (address, telephone, geo, @id) bleiben unveraendert erhalten"
  artifacts:
    - path: "public/index.html"
      provides: "Vollstaendiges Optician-Schema + WebSite-Schema"
      contains: "openingHoursSpecification"
  key_links:
    - from: "public/index.html Optician-Schema"
      to: "https://www.google.com/maps?cid=13745904768855660490"
      via: "sameAs Array"
      pattern: "13745904768855660490"
    - from: "public/index.html Optician-Schema"
      to: "areaServed City-Objekte"
      via: "areaServed Array"
      pattern: "@type.*City"
---

<objective>
Optician-Schema auf index.html vervollstaendigen und WebSite-Schema hinzufuegen — die zwei Schema-Luecken auf der Homepage schliessen die fuer Google Knowledge Panel entscheidend sind.

Purpose: Ohne openingHoursSpecification (korrekt), sameAs (Maps-CID) und areaServed (City-Objekte) kann Google kein vollstaendiges Knowledge Panel aufbauen. WebSite-Schema signalisiert Sprache und Identitaet der Website.
Output: Erweiterter Optician-Schema-Block + neuer WebSite-Block in public/index.html.
</objective>

<execution_context>
@/Users/claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schema/03-CONTEXT.md
@.planning/phases/03-schema/03-RESEARCH.md

<interfaces>
<!-- Bestehender Optician-Schema-Block in index.html (Zeilen 60-92) -->
<!-- Exakter aktueller Stand: -->

```json
{
  "@context": "https://schema.org",
  "@type": "Optician",
  "name": "Besser Sehen Landshut",
  "image": "https://bessersehen.la/bilder/startseite-hero.jpg",
  "@id": "https://bessersehen.la",
  "url": "https://bessersehen.la",
  "telephone": "+4987114228020",
  "priceRange": "€€",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Veldener Str. 16",
    "addressLocality": "Landshut",
    "postalCode": "84036",
    "addressCountry": "DE"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 48.5239261,
    "longitude": 12.1449762
  },
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
    "opens": "09:00",
    "closes": "18:00"
  },
  "sameAs": [
    "https://www.facebook.com/bessersehenlandshut/"
  ]
}
```

<!-- KRITISCH: closes muss von "18:00" auf "16:00" korrigiert werden -->
<!-- KRITISCH: openingHoursSpecification muss ein Array werden -->
<!-- Google Maps CID Decimal: 13745904768855660490 (aus Hex 0xbec3438eeffd57ca) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optician-Schema erweitern — openingHours korrigieren, sameAs + areaServed ergaenzen</name>
  <files>public/index.html</files>
  <action>
Den bestehenden Optician-Schema-Block in public/index.html (Zeilen 60-92) in-place ersetzen. Den gesamten `<script type="application/ld+json">...Optician-Block...</script>` durch die erweiterte Version ersetzen.

ERSETZE den kompletten Optician-Schema-Block durch:

```json
{
  "@context": "https://schema.org",
  "@type": "Optician",
  "name": "Besser Sehen Landshut",
  "image": "https://bessersehen.la/bilder/startseite-hero.jpg",
  "@id": "https://bessersehen.la",
  "url": "https://bessersehen.la",
  "telephone": "+4987114228020",
  "priceRange": "€€",
  "description": "Ihr Experte fuer spezielle Kontaktlinsen, Myopie-Management, Nachtlinsen (Orthokeratologie) und visuelle Rehabilitation in Landshut. Beratung auch nach Vereinbarung moeglich.",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Veldener Str. 16",
    "addressLocality": "Landshut",
    "postalCode": "84036",
    "addressCountry": "DE"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 48.5239261,
    "longitude": 12.1449762
  },
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
      "opens": "09:00",
      "closes": "16:00"
    }
  ],
  "areaServed": [
    {"@type": "City", "name": "Landshut"},
    {"@type": "City", "name": "Ergolding"},
    {"@type": "City", "name": "Rottenburg an der Laaber"}
  ],
  "sameAs": [
    "https://www.facebook.com/bessersehenlandshut/",
    "https://www.google.com/maps?cid=13745904768855660490"
  ]
}
```

Kritische Details:
- `openingHoursSpecification` wird ein Array (eckige Klammer aussen) — auch wenn nur ein Objekt drin ist
- `"closes": "18:00"` → `"closes": "16:00"` (bestehender Fehler korrigieren)
- `areaServed` als Array von City-Objekten, NICHT als String "Landshut"
- Google Maps CID Decimal: 13745904768855660490 (aus Hex 0xbec3438eeffd57ca — bereits berechnet)
- `description` ergaenzt "nach Vereinbarung" als Text statt proprietaeres Feld (per RESEARCH.md-Empfehlung)
- Alle bestehenden Felder (@id, address, geo, telephone, priceRange, image) unveraendert beibehalten
- Einrueckung und Struktur des Blocks: 1 Tab Einrueckung wie im Original beibehalten
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
with open('/Users/claude/Projekte/BesserSehenLandshut/public/index.html') as f:
    content = f.read()
start = content.find('\"@type\": \"Optician\"')
block_start = content.rfind('<script', 0, start)
block_end = content.find('</script>', start) + 9
schema_text = content[block_start:block_end]
json_start = schema_text.find('{')
json_end = schema_text.rfind('}') + 1
data = json.loads(schema_text[json_start:json_end])
assert data['openingHoursSpecification'][0]['closes'] == '16:00', 'Schliesszeit muss 16:00 sein'
assert isinstance(data['openingHoursSpecification'], list), 'openingHoursSpecification muss Array sein'
assert any('maps' in s for s in data['sameAs']), 'Google Maps URL fehlt in sameAs'
assert isinstance(data['areaServed'], list), 'areaServed muss Array sein'
assert data['areaServed'][0].get('@type') == 'City', 'areaServed muss City-Objekte enthalten'
print('OK: Optician-Schema valide')
"
</automated>
  </verify>
  <done>
Optician-Schema-Block in index.html erhaelt: openingHoursSpecification als Array mit closes=16:00, sameAs mit Facebook + Google Maps CID, areaServed mit 3 City-Objekten (Landshut, Ergolding, Rottenburg an der Laaber), description mit Hinweis auf Terminvereinbarung. Alle bestehenden Pflichtfelder bleiben erhalten.
  </done>
</task>

<task type="auto">
  <name>Task 2: WebSite-Schema als neuen Block nach Optician-Block einfuegen</name>
  <files>public/index.html</files>
  <action>
Direkt nach dem schliessenden `</script>`-Tag des Optician-Schema-Blocks einen neuen separaten `<script type="application/ld+json">`-Block einfuegen.

Inhalt des neuen Blocks (ohne SearchAction — per CONTEXT.md locked: statische Seite):

```html
	<script type="application/ld+json">
	{
	  "@context": "https://schema.org",
	  "@type": "WebSite",
	  "name": "Besser Sehen Landshut",
	  "url": "https://bessersehen.la",
	  "inLanguage": "de-DE"
	}
	</script>
```

Details:
- Einrueckung: 1 Tab wie im bestehenden Optician-Block
- KEIN SearchAction (per CONTEXT.md: statische Seite, SearchAction ohne echte Suche kann Google-Penalty verursachen)
- `inLanguage: "de-DE"` — mit Bindestrich, nicht Unterstrich (de_DE ist fuer og:locale)
- Positionierung: Unmittelbar nach dem schliessenden `</script>` des Optician-Blocks, vor der naechsten CSS-Link-Zeile
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
with open('/Users/claude/Projekte/BesserSehenLandshut/public/index.html') as f:
    content = f.read()
idx = content.find('\"@type\": \"WebSite\"')
assert idx != -1, 'WebSite-Schema fehlt'
block_start = content.rfind('<script', 0, idx)
block_end = content.find('</script>', idx) + 9
schema_text = content[block_start:block_end]
json_start = schema_text.find('{')
json_end = schema_text.rfind('}') + 1
data = json.loads(schema_text[json_start:json_end])
assert data['@type'] == 'WebSite', '@type muss WebSite sein'
assert data['inLanguage'] == 'de-DE', 'inLanguage muss de-DE sein'
assert 'SearchAction' not in schema_text, 'SearchAction darf nicht vorhanden sein'
print('OK: WebSite-Schema valide')
"
</automated>
  </verify>
  <done>
Neuer WebSite-Schema-Block direkt nach dem Optician-Block in index.html mit @type=WebSite, name, url, inLanguage=de-DE. Kein SearchAction-Feld. Beide Schema-Bloecke sind valides JSON.
  </done>
</task>

</tasks>

<verification>
Gesamtpruefung nach Abschluss beider Tasks:

```bash
# Pruefen ob beide Schema-Typen in index.html vorhanden sind:
grep -c '"@type": "Optician"\|"@type": "WebSite"' /Users/claude/Projekte/BesserSehenLandshut/public/index.html

# Pruefen ob Schliesszeit korrekt ist (nicht 18:00):
grep "closes" /Users/claude/Projekte/BesserSehenLandshut/public/index.html

# Google Maps CID vorhanden:
grep "13745904768855660490" /Users/claude/Projekte/BesserSehenLandshut/public/index.html

# areaServed als City-Objekte:
grep -c '"@type": "City"' /Users/claude/Projekte/BesserSehenLandshut/public/index.html
```
</verification>

<success_criteria>
1. Optician-Schema enthaelt openingHoursSpecification (Array, closes=16:00), sameAs (Facebook + Maps CID 13745904768855660490), areaServed (3 City-Objekte), description
2. WebSite-Schema-Block vorhanden mit @type=WebSite, inLanguage=de-DE, kein SearchAction
3. Beide Bloecke sind valides JSON (kein Syntaxfehler)
4. Alle bestehenden Felder (address, geo, telephone, @id, priceRange) unveraendert
</success_criteria>

<output>
Nach Abschluss, erstelle `.planning/phases/03-schema/03-01-SUMMARY.md` gemaess dem Summary-Template.
</output>
